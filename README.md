# Laurel's Lost and Found

A bare-bones Astro site of walks.

- `npm install`, then `npm run dev` to run locally, `npm run build` to build to `dist/`.
- Each walk is a Markdown file in `src/content/walks/` with `date`, `duration` (minutes) and `path` (an SVG in `public/paths/`).
