---
import { getCollection } from 'astro:content';
import { walkTitle } from '../walkTitle';

// Newest first. Ids are dates (plus -2, -3 for extra walks that day), so they sort as text.
const walks = (await getCollection('walks')).sort((a, b) => b.id.localeCompare(a.id));
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Lost &amp; Found</title>
  </head>
  <body>
    <h1>Lost &amp; Found</h1>
    <ul class="walks">
      {walks.map((walk) => (
        <li>
          <a class="walk" href={`/walks/${walk.id}/`}>
            <img src={walk.data.path} alt="" />
            <span class="date">{walkTitle(walk.data.date)}</span>
          </a>
        </li>
      ))}
    </ul>
  </body>
</html>

<style>
  .walks {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .walk {
    position: relative;
    display: block;
    width: 300px;
    aspect-ratio: 1;
    background: #eee;
    color: inherit;
    text-decoration: none;
    font-family: 'Courier New', Courier, monospace;
    font-size: 16px;
    transition: background 0.25s ease;
  }

  .walk:hover {
    background: #e4e4e4;
  }

  /* The path sits inside the box, clear of the date. */
  .walk img {
    position: absolute;
    inset: 32px;
    width: calc(100% - 64px);
    height: calc(100% - 64px);
    object-fit: contain;
  }

  .date {
    position: absolute;
    top: 6px;
    left: 6px;
  }
</style>
